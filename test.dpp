var a = 2
var i = 2 + a * 2 / 8 + a
a = i + a

draw line(1+1, a * 8)

if(a == True){
    draw square(i, a, 1, 2+5)
}

for(var inc = 0; inc < 5; inc = inc +1) {
    color("pink")
}

while (True) {
    draw line(1,2)
}